"Train" and "Test" data have been combined into a single file (audiology.standarized.data),
since we perform the train/test split internally.

Also, the identifier has been removed since it is not relevant for classification.

